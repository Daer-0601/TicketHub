@model ProyectoFinal.Models.Event
@{
    var disponibilidad = ViewBag.Disponibilidad as Dictionary<int, int> ?? new Dictionary<int, int>();
}

<h2>@Model.Name</h2>

<div class="row">
    <div class="col-md-6">
        <img src="@Model.ImageUrl" class="img-fluid rounded">
    </div>

    <div class="col-md-6">
        <h4>Selecciona tus tickets</h4>

        @if (TempData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="bi bi-exclamation-triangle-fill"></i> @TempData["ErrorMessage"]
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        }

        <form asp-action="Buy" method="post">
            <input type="hidden" name="eventId" value="@Model.EventId">

            <div class="mb-3">
                <label>Email donde recibirás los tickets</label>
                <input type="email" name="email" required class="form-control" />
            </div>

            @foreach (var s in Model.Sectors)
            {
                int entradasDisponibles = disponibilidad.ContainsKey(s.SectorId) ? disponibilidad[s.SectorId] : s.Capacity;
                bool agotado = entradasDisponibles <= 0;

                <div class="card p-3 mb-2 @(agotado ? "bg-light" : "")">
                    <div class="d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">@s.Name ($@s.Price)</h5>
                        @if (agotado)
                        {
                            <span class="badge bg-danger">AGOTADO</span>
                        }
                        else
                        {
                            <span class="badge bg-success">@entradasDisponibles disponibles</span>
                        }
                    </div>
                    <label class="mt-2">Cantidad:</label>
                    <input type="number" 
                           min="0" 
                           max="@entradasDisponibles" 
                           value="0" 
                           name="cantidades[@s.SectorId]" 
                           class="form-control" 
                           @(agotado ? "disabled" : "") />
                </div>
            }

            <button class="btn btn-success mt-3">Comprar</button>
        </form>
    </div>
</div>
