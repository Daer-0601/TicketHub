@model IEnumerable<ProyectoFinal.Models.Event>
@{
    Layout = "~/Views/Shared/_LayoutClient.cshtml";
    ViewData["Title"] = "Events";
    var months = new[] { "JAN", "FEB", "MAR", "APR", "MAY", "JUN", "JUL", "AUG", "SEP", "OCT", "NOV", "DEC" };
}

<section class="hero-section">
    <div class="hero-content">
        <h1 class="hero-title">Find the best <span>events</span></h1>
        <p class="hero-subtitle">Discover concerts, theater, sports and more. Buy your tickets securely.</p>
        
        <div class="search-box">
            <input type="text" id="searchInput" placeholder="Search events, artists, venues..." />
            <button type="button" onclick="filterEvents()">
                <i class="bi bi-search"></i> Search
            </button>
        </div>
    </div>
</section>

<section class="events-section">
    <div class="section-header">
        <h2 class="section-title">Upcoming Events</h2>
        <p class="section-subtitle">Don't miss any event. Buy your tickets now!</p>
    </div>

    @if (Model.Any())
    {
        <div class="events-grid" id="eventsGrid">
            @foreach (var evento in Model.OrderBy(e => e.Date))
            {
                var minPrice = evento.Sectors?.Any() == true ? evento.Sectors.Min(s => s.Price) : 0;
                
                <div class="event-card" data-name="@evento.Name.ToLower()" data-location="@evento.Location.ToLower()">
                    <div class="event-card-image">
                        @if (!string.IsNullOrEmpty(evento.ImageUrl))
                        {
                            <img src="@evento.ImageUrl" alt="@evento.Name" />
                        }
                        else
                        {
                            <img src="https://via.placeholder.com/400x220/1a1a2e/ffffff?text=@evento.Name" alt="@evento.Name" />
                        }
                        
                        <div class="date-badge">
                            <span class="day">@evento.Date.Day</span>
                            <span class="month">@months[evento.Date.Month - 1]</span>
                        </div>
                       
                        
                        <span class="category-tag">
                            <i class="bi bi-ticket-perforated"></i> Event
                        </span>
                    </div>
                    
                    <div class="event-card-body">
                        <h3 class="event-card-title">@evento.Name</h3>
                        
                        <div class="event-card-info">
                            <div class="event-card-info-item">
                                <i class="bi bi-calendar-event"></i>
                                <span>@evento.Date.ToString("dddd, MMMM dd, yyyy", new System.Globalization.CultureInfo("en-US"))</span>
                            </div>
                            <div class="event-card-info-item">
                                <i class="bi bi-clock"></i>
                                <span>@evento.Time.ToString(@"hh\:mm") hrs</span>
                            </div>
                            <div class="event-card-info-item">
                                <i class="bi bi-geo-alt"></i>
                                <span>@evento.Location</span>
                            </div>
                        </div>
                        
                        <div class="event-card-footer">
                            <div class="event-price">
                                From
                                <span>$@minPrice.ToString("N0")</span>
                            </div>
                            <a asp-action="Details" asp-route-id="@evento.EventId" class="btn-tickets">
                                Buy <i class="bi bi-arrow-right"></i>
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="empty-state">
            <i class="bi bi-calendar-x"></i>
            <h3>No events available</h3>
            <p>Come back soon to see new exciting events.</p>
        </div>
    }
</section>

@section Scripts {
    <script>

        function filterEvents() {
            const searchTerm = document.getElementById('searchInput').value.toLowerCase();
            const cards = document.querySelectorAll('.event-card');
            
            cards.forEach(card => {
                const name = card.dataset.name;
                const location = card.dataset.location;
                
                if (name.includes(searchTerm) || location.includes(searchTerm)) {
                    card.style.display = 'block';
                } else {
                    card.style.display = 'none';
                }
            });
        }
        
     
        document.getElementById('searchInput').addEventListener('keyup', function(e) {
            if (e.key === 'Enter') {
                filterEvents();
            }
        });
        
        
        });
    </script>
}
